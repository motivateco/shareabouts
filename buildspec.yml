version: 0.2

env:
  variables:

phases:
  install:
    commands:
      - apt-get install apache2-utils libapache2-mod-wsgi build-essential git libpq-dev
      - pip install -r requirements.txt
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase...
  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
  discard-paths: yes
